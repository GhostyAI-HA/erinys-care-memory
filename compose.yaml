services:
  erinys-care-memory:
    build: .
    image: erinys-care-memory:latest
    container_name: erinys-care-memory
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "80:8000"
    volumes:
      - erinys-care-memory-data:/data

volumes:
  erinys-care-memory-data:
